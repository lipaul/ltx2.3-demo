# B60 & B70 运行指南 — LTX-2.3 / LTX-2.5 视频生成

本仓库是一个薄封装：模型代码在 gitignored 的 `LTX-2/`（以 editable 方式从
`LTX-2/packages/{ltx-core,ltx-pipelines}` 安装）。本指南覆盖 B60 多卡节点与
单张 B70 两种运行环境，命令均可直接复制执行。

> 相关文档：`README.txt`（完整性能与实验历史）、`AGENTS.md`（架构细节）、
> `B70使用说明.md`（B70 单机速查，含 thermal/故障排查）。

---

## 0. 环境准备（两种机器通用）

```bash
git clone <repo>            # 或使用已有仓库
cd ltx2.3-demo
bash setup_env.sh           # 克隆 LTX-2（固定 rev）、打 XPU 补丁、uv sync、
                            # editable 安装 ltx-core/ltx-pipelines、编译 R8 SYCL 核
uv run download.py          # 下载 LTX-2.3 权重到 models/（~30 GB）
```

校验解释器与设备：

```bash
.venv/bin/python -c "import torch; print(torch.__version__, torch.xpu.is_available(), torch.xpu.device_count())"
```

- Python **3.12**，解释器统一用 `.venv/bin/python`（可用 `LTX_PYTHON` 覆盖）。
  直接跑 `python`/`python3` 是 3.10，无法导入这些包。
- `LTX-2/`、`models/`、`.venv/` 均在 `.gitignore` 中，clone 后必须执行上面的
  `setup_env.sh` + `download.py`。
- `LTX_SKIP_KERNELS=1` 可跳过 SYCL 编译；缺 `libsycl.so.8`（2025.x oneAPI）时
  R8/R9A 自动回退 eager（不影响正确性，略慢）。

---

## 1. 关键前提：用 `LTX_PROFILE` 选择设备布局

单路生成的三类模型（transformer / VAE+解码器 / 文本编码器）需要放在合适的
XPU 上。`LTX_PROFILE` 提供两套默认布局：

| `LTX_PROFILE` | transformer | VAE/解码器 | 文本编码器（流式 Gemma） | 适用机器 |
|---|---|---|---|---|
| `b60dual`（默认） | `xpu:1` | `xpu:0` | `xpu:0` | 多卡 B60 节点 |
| `b70` | `xpu:0` | `xpu:0` | `xpu:0` | 单张 B70（30 GiB） |

- 显式设置 `LTX_TDEV` / `LTX_CDEV` / `LTX_GEMMA_DEVICE` 会**覆盖** profile。
- 未知 profile 会立即报错退出。
- **重要**：不要把 transformer + VAE + Gemma 全部放在同一张 24 GB B60 上。
  流式 Gemma 权重（bf16 ~20 GB）会占用整卡，紧随其后的 embeddings processor
  构建会在同一张卡上 OOM。`b60dual` 把 transformer 单独放在 `xpu:1` 正是为此。
  （文本编码先于生成执行，Gemma 与 VAE 共用 `xpu:0` 不会重叠。）

B70 上 `LTX_CDEV=1` 不存在（只有 1 个 XPU），所以必须用 `LTX_PROFILE=b70`
或显式 `LTX_TDEV=LTX_CDEV=0`。

> 注：`LTX_PROFILE` 只作用于 2.3 单路（`run_t2v_xpu_perf.py`）。LTX-2.5 的
> `run_t2v_25_xpu.py` 是单设备管线，用 `LTX_TDEV`（默认 `xpu:0`）选卡，不读 profile。

---

## 2. 完整运行命令

### 2.1 LTX-2.3 单路生成（B60 多卡）

```bash
cd <repo>
HF_HUB_OFFLINE=1 LTX_PROFILE=b60dual \
  .venv/bin/python -u run_t2v_xpu_perf.py
```

- 输出默认：`<repo>/output/output_1024.mp4`。
- `LTX_TDEV=1 LTX_CDEV=0`（transformer/VAE 分卡）由 `b60dual` 自动设定。

### 2.2 LTX-2.3 单路生成（B70 单卡）

```bash
cd <repo>
HF_HUB_OFFLINE=1 LTX_PROFILE=b70 \
  .venv/bin/python -u run_t2v_xpu_perf.py
```

等价于 `LTX_TDEV=0 LTX_CDEV=0 LTX_GEMMA_DEVICE=xpu:0`。

### 2.3 自定义提示词 / 分辨率 / 帧数

```bash
# 便捷脚本（默认 1024x1024, 121 帧）
./run.sh "your prompt here"
LTX_PROFILE=b70 ./run.sh "your prompt here"          # B70

# 小分辨率快速验证（分辨率必须能被 64 整除；帧数必须为 8k+1）
LTX_WIDTH=512 LTX_HEIGHT=512 LTX_FRAMES=41 \
  LTX_OUTPUT_PATH=/tmp/out.mp4 \
  .venv/bin/python -u run_t2v_xpu_perf.py
```

- **分辨率必须能被 64 整除**；stage-1 为其一半。
- **帧数必须满足 8k+1；切勿用 73（会挂 XPU 驱动）**。默认 121 帧 @ 24 fps ≈ 5 s。
- 其它常用变量：`LTX_PROMPT`、`LTX_OUTPUT_PATH`、`LTX_EMBEDDINGS_PATH`、
  `LTX_GEMMA_DEVICE`、`LTX_GEMMA_OFFLOAD`、`LTX_DECODER_MEM_EFFICIENT`、
  `LTX_PREBUILD_TRANSFORMER`。

### 2.4 多路生成（仅 B60 多卡）

```bash
# 8 路（worker i -> xpu:2i / xpu:2i+1）
.venv/bin/python run_multi_xpu.py --prompts-file prompts.json --job-dir OUT

# 16 路
.venv/bin/python run_multi_16.py --prompts-file prompts.json --job-dir OUT
```

- 先用一张 Gemma 预编码全部提示词，再并发拉起子进程加载 embeddings。
- `LTX_PROFILE=b70` 时多路会退化为同一张 `xpu:0` 上顺序写入——单张 B70 请用
  单路 runner（2.2）。
- 可选 `LTX_SPAWN_DELAY`（默认 1 s）错峰启动，规避并发初始化的驱动竞争。

### 2.5 常驻 Web 服务

```bash
# LTX-2.3，B60 多卡（:8001）
LTX_MULTI_MODE=16 LTX_HOST=0.0.0.0 LTX_API_TOKEN=<token> \
  .venv/bin/python ltx_server.py

# LTX-2.3，B70 单卡（必须 LTX_MULTI_MODE=1）
LTX_PROFILE=b70 LTX_MULTI_MODE=1 LTX_HOST=127.0.0.1 \
  .venv/bin/python ltx_server.py

# LTX-2.5 单路服务（:8002，单卡）
bash start_ltx_server_25.sh                          # B70（30 GiB）

# B60（24 GiB）必须释放 transformer 再解码，否则解码阶段 OOM：
LTX_KEEP_TRANSFORMER=0 bash start_ltx_server_25.sh   # B60
```

无 UI 提交任务：

```bash
curl -s -X POST http://127.0.0.1:8001/api/multi-jobs \
  -H "Authorization: Bearer <token>" -H "Content-Type: application/json" \
  -d '{"prompts": ["... prompts ..."]}'
```

- 输出：`$LTX_OUTPUT_DIR/<job_id>/video_i.mp4`，每 worker 日志 `video_i.log`。
- 进度：SSE `/api/events`；`/api/multi-jobs/{id}/videos/{i}` 端点**不做鉴权**。

### 2.6 LTX-2.5 单路生成（B60 / B70 通用）

2.5 用独立 runner（单设备管线），权重可放在仓库外，用 `LTX_25_MODELS` 指向
split pack 根目录（目录内需含 `diffusion_models/`、`text_encoders/`、`vae/`、
`model_patches/`、`latent_upscale_models/`）。

```bash
cd <repo>
# B70（30 GiB）：transformer 常驻即可
HF_HUB_OFFLINE=1 LTX_25_MODELS=/path/to/ltx-2.5 \
  .venv/bin/python -u run_t2v_25_xpu.py

# B60（24 GiB）：stage-2 后释放 transformer，否则 VAE 解码阶段 OOM
HF_HUB_OFFLINE=1 LTX_25_MODELS=/path/to/ltx-2.5 LTX_KEEP_TRANSFORMER=0 \
  .venv/bin/python -u run_t2v_25_xpu.py
```

- **为什么 B60 要加 `LTX_KEEP_TRANSFORMER=0`**：默认（=1）会把 fp8 transformer
  （~19 GB）缓存到解码结束；24 GB 的 B60 上「常驻 transformer + 121 帧 1024²
  解码」会 OOM（`XPU out of memory ... 23.91 GiB ... 250.22 MiB is free`）。
  设为 0 后 stage-2 的 `gpu_model` 上下文会释放 transformer，解码独占整卡；
  代价是 stage-2 需重建一次 transformer（~6 s）。B70（30 GiB）不需要设。
- 输出默认 `<repo>/output/`，可用 `LTX_OUTPUT_PATH` 覆盖。
- 2.5 忽略 `LTX_PROFILE`，用 `LTX_TDEV`（默认 `xpu:0`）选卡。

---

## 3. 实测结果与各阶段时间

以下时间以 `run_t2v_xpu_perf.py` 的 PERFORMANCE SUMMARY 为口径，seed 42、
默认提示词。来源已分别标注。

### 3.1 B60 双卡（`xpu:30/31`）1024×1024 / 121 帧 — 来源：README.txt

| 阶段 | 时间 |
|---|---:|
| prompt-encode（Gemma-3，流式 `xpu:0`） | 19.10 s |
| stage-1 denoise（8 步 @512²） | 39.30 s |
| spatial-upsample 2× | 2.00 s |
| stage-2 denoise（3 步 @1024²） | 39.00 s |
| video+audio decode | 14.20 s |
| mux to mp4（含惰性 VAE 视频解码） | 18.30 s |
| **合计** | **131.80 s** |
| 峰值显存 | 18.2 GB + 0.8 GB（两卡） |

### 3.2 单张 B70（30 GiB）1024×1024 / 121 帧 — 来源：README.txt

| 阶段 | 时间 |
|---|---:|
| prompt-encode（`xpu:0`） | 9.54 s |
| stage-1 denoise（8 步 @512²） | 17.90 s |
| spatial-upsample 2× | 0.67 s |
| stage-2 denoise（3 步 @1024²） | 22.09 s |
| video+audio decode | 5.38 s |
| mux to mp4 | 12.46 s |
| **合计** | **68.05 s** |
| 峰值显存 | 18.15 GB（单卡） |

**B60 vs B70 对比**（同 1024²/121、默认提示词）：

| 阶段 | B60 | B70 | 加速 |
|---|---:|---:|---:|
| prompt-encode | 19.10 s | 9.54 s | 2.00× |
| stage-1 denoise | 39.30 s | 17.90 s | 2.20× |
| spatial-upsample 2× | 2.00 s | 0.67 s | 2.99× |
| stage-2 denoise | 39.00 s | 22.09 s | 1.77× |
| video+audio decode | 14.20 s | 5.38 s | 2.64× |
| mux to mp4 | 18.30 s | 12.46 s | 1.47× |
| **合计** | **131.80 s** | **68.05 s** | **1.94×** |
| 峰值显存 | 18.2+0.8 GB | 18.15 GB | — |

> 注：B60 为双卡（transformer/VAE 分卡，有跨卡搬运），B70 为单卡；两者主机
> CPU/内存、驱动版本不同，且各只有一次采样，对比仅供参考。

### 3.3 本机 B60 实测（`LTX_PROFILE=b60dual`）512×512 / 41 帧 — 本次实测

命令：

```bash
HF_HUB_OFFLINE=1 LTX_PROFILE=b60dual LTX_FRAMES=41 LTX_WIDTH=512 LTX_HEIGHT=512 \
  LTX_OUTPUT_PATH=/tmp/ltx_verify.mp4 \
  .venv/bin/python -u run_t2v_xpu_perf.py
```

设备布局与耗时（`transformer=xpu:1`，`vae/decoders=xpu:0`，`text-encoder=xpu:0`）：

| 阶段 | 时间 | 占比 |
|---|---:|---:|
| prompt-encode（`xpu:0`） | 16.56 s | 46.3% |
| stage-1 denoise（8 步 @256²） | 7.00 s | 19.6% |
| spatial-upsample 2× | 0.66 s | 1.8% |
| stage-2 denoise（3 步 @512²） | 4.18 s | 11.7% |
| video+audio decode | 6.46 s | 18.1% |
| mux to mp4 | 0.90 s | 2.5% |
| **TOTAL** | **35.77 s** | 100% |
| 峰值 `xpu:0` | 0.77 GB | — |
| 峰值 `xpu:1` | 18.14 GB | — |

- 退出码 0，输出 193,723 字节的有效 mp4。
- transformer 独占 `xpu:1`（峰值 ~18.1 GB），VAE 与文本编码器共享 `xpu:0`。
- 小分辨率下 prompt-encode 占比偏高（固定开销），分辨率/帧数增大后
  denoise 阶段占比会上升。

### 3.4 LTX-2.5（可选，单卡）1024×1024 / 121 帧 — 来源：README.txt / B70使用说明.md

| 阶段 | 时间 |
|---|---:|
| Gemma-4 TE 构建 | ~8.6 s |
| embeddings processor + prompt 编码 | ~2.5 s |
| transformer fp8-cast 构建 | ~6.4 s |
| stage-1 denoise（8 步 @512²） | 14.4 s（1.80 s/步） |
| stage-2 denoise（3 步 @1024²）+ decode | ~27 s（7.3 s/步 + ~5 s decode） |
| mux to mp4 | ~5.2 s（去 tiling；原 7.0 s） |
| **generation 计时** | **~56 s** |
| wall（启动→落盘） | ~63 s |
| 峰值显存 | 25.28 GB |

- 2.5 bf16 transformer 为 42 GB，载入时 fp8-cast 至 ~21 GB；Gemma-4 流式。
- 仅启用逐位安全的优化（`LTX_R2_NOTILE` + `LTX_R5_FUSE` + `LTX_R9A_FP8K`）。
- **2.5 价值在质量/功能，不是速度**：同 8+3 调度下与 2.3 速度相近，但显存更高。

### 3.5 本机 B60 实测 LTX-2.5（`LTX_TDEV=0`，单卡）1024×1024 / 121 帧 — 本次实测

命令（权重在 `~/work/models/ltx-2.5`）：

```bash
HF_HUB_OFFLINE=1 LTX_25_MODELS=/home/lm/work/models/ltx-2.5 \
  LTX_KEEP_TRANSFORMER=0 \
  LTX_OUTPUT_PATH=<repo>/output/output_25_b60.mp4 \
  .venv/bin/python -u run_t2v_25_xpu.py
```

| 阶段 | 时间 |
|---|---:|
| Gemma-4 TE 构建 + embeddings + prompt 编码 | ~14.5 s |
| transformer fp8-cast 构建（stage-1） | ~11.6 s |
| stage-1 denoise（8 步 @512²） | 23.1 s（2.85 s/步） |
| transformer 重建（stage-2，`KEEP_TRANSFORMER=0`） | ~5.5 s |
| stage-2 denoise（3 步 @1024²） | 38.1 s（12.6 s/步） |
| video+audio decode + mux | 13.3 s |
| **generation 计时** | **101.2 s** |
| wall（启动→落盘） | ~115 s |
| 峰值 reserved | 22.68 GB |

- 输出：`output/output_25_b60.mp4`，1.53 MB，h264，1024×1024，121 帧，24 fps，5.04 s。
- 直接跑（`LTX_KEEP_TRANSFORMER=1`）时生成成功、解码 OOM；加 `=0` 后通过
  （transformer 在 stage-2 后释放，解码独占 24 GB）。
- 相比 30 GiB B70 的 wall ~63 s，B60 约 **1.8×** 慢（stage-2 每步 12.6 vs 7.3 s）。

---

## 4. 常见问题

| 现象 | 原因 / 处理 |
|---|---|
| OOM（`XPU out of memory` / `UR_RESULT_ERROR_OUT_OF_RESOURCES`），发生在 embeddings processor 构建阶段 | transformer+VAE+Gemma 挤在同一张 B60。用 `LTX_PROFILE=b60dual`（transformer 放 `xpu:1`），或显式分卡。 |
| OOM，发生在 **LTX-2.5 的 VAE 解码阶段**（`conv3d` 申请 ~1 GiB 失败） | 24 GB B60 上 fp8 transformer 常驻挤占显存。加 `LTX_KEEP_TRANSFORMER=0`，让 stage-2 后释放 transformer 再解码（B70 不需要）。 |
| `xpu:1` 不存在 / `LTX_CDEV=1` 失败 | 单卡机器，使用 `LTX_PROFILE=b70`（全部 `xpu:0`）。 |
| `UR_RESULT_ERROR_DEVICE_LOST` / `exec queue reset` / 任务中途挂死 | XPU 驱动瞬时状态。冷却或**冷启动**后再试；2.5 服务对信号崩溃的任务会自动重试 1 次。 |
| `LTX_FRAMES=73` 导致驱动挂死 | 帧数必须 8k+1；改用 41/81/121。 |
| 并发 16 路初始化不稳定 | 提高 `LTX_SPAWN_DELAY`，或冷启动恢复驱动。 |
| 首次运行很慢 | `torch.compile`（`run_t2v_compiled.sh`）首次 JIT ~9 s，之后复用 `.torch_cache/`。 |
| 残留进程占满显存 | `ps -eo pid,cmd \| grep -E "run_t2v\|ltx_server\|encode_service"` 后 kill。 |

### B70 温度检查

```bash
for f in /sys/bus/pci/devices/0000:05:00.0/hwmon/hwmon*/temp2_input; do
  echo "$f = $(( $(cat "$f") / 1000 )) °C"
done
```

---

## 5. 端口与文件一览

| 项 | 位置 / 端口 |
|---|---|
| LTX-2.3 服务 | `:8001`（`ltx_server.py`） |
| LTX-2.5 服务 | `:8002`（`ltx_server_25.py`） |
| 单路输出 | `<repo>/output/` |
| 服务输出/日志 | `outputs/ltx-server/`、`outputs/ltx-server-25/` |
| LTX-2.3 权重 | `<repo>/models/` |
| LTX-2.5 split pack | `<repo>/models/ltx-2.5/`（或 `LTX_25_MODELS`，如本机 `~/work/models/ltx-2.5`） |
| 补丁 | `patches/xpu.patch`、`patches/r_kernels.patch` |
