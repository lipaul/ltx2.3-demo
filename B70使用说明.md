# B70 使用说明 — LTX-2.3 / LTX-2.5 视频生成

本机：单张 **Intel Arc Pro B70**（约 30.3 GiB 显存）。torch 在本机只暴露 **1 个 XPU**
（`torch.xpu.device_count() == 1`），因此所有单路运行都必须 **`LTX_TDEV=LTX_CDEV=0`**
（默认 `LTX_CDEV=1` 会失败）。

仓库根目录（下称 `<repo>`）：`/home/acm/paul_arc/ltx2.3-demo`

---

## 0. 从 GitHub 克隆后从零恢复（fresh clone → 出片）

### 0.1 前置依赖
- Linux（Ubuntu）；**git**、**uv**（`~/.local/bin/uv`）、**Python 3.12**（uv 可自动下载）。
- Intel **oneAPI 2025.x**（提供含 `libsycl.so.8` 的 `icpx`）——**仅 R8/R9A 自写 SYCL 核需要**；
  缺失时自动回退 eager（不影响正确性，速度略降）。
- 磁盘建议 ≥ ~150 GB 富余（2.3 权重 ~30–45 GB、2.5 权重 ~70 GB、`.venv` 若干 GB）。
- 网络可达 GitHub / PyPI / `download.pytorch.org/whl/xpu` / HuggingFace。

### 0.2 克隆并建环境
```bash
git clone https://github.com/lipaul/ltx2.3-demo.git
cd ltx2.3-demo
bash setup_env.sh
```
`setup_env.sh` 会依次：克隆 `LTX-2`（固定 rev，`--detach -f` 强制重置）、打 XPU 补丁
（`patches/xpu.patch` 等价项 + `patches/r_kernels.patch`）、`uv sync`（建 `.venv`，装 torch+xpu）、
可编辑安装 `ltx-core`/`ltx-pipelines`、编译 `sycl_r8`。
`LTX_SKIP_KERNELS=1` 可跳过 SYCL 编译。

校验：
```bash
.venv/bin/python -c "import torch; print(torch.__version__, torch.xpu.is_available(), torch.xpu.device_count())"
```

### 0.3 下载权重
LTX-2.3（`download.py` 自动下载到 `models/`，~30–45 GB）：
```bash
uv run download.py
```
LTX-2.5（可选，split pack，~72 GB；放到 `models/ltx-2.5/`）：
```bash
hf download Lightricks/LTX-2.5 \
  diffusion_models/ltx-2.5-22b-distilled-transformer-bf16.safetensors \
  text_encoders/gemma4-12b-with-proj-ltx-2.5-bf16.safetensors \
  vae/ltx-2.5-video-vae-conv-bf16.safetensors \
  vae/ltx-2.5-audio-vae-bf16.safetensors \
  model_patches/ltx-2.5-duration-head-bf16.safetensors \
  latent_upscale_models/ltx-2.5-latent-spatial-upscaler-x2-bf16-1.0.safetensors \
  --local-dir models/ltx-2.5
```
- 2.5 默认从 `<repo>/models/ltx-2.5` 读取（`LTX_25_MODELS` 可指向其它路径）；缺文件时 runner 会列出缺失项。
- 权重在别处可软链：`ln -s <your-models>/ltx-2.5 models/ltx-2.5`。
- `models/`、`.venv/`、`LTX-2/` 均在 `.gitignore` 中，**clone 后不存在，必须执行 0.2/0.3**。

### 0.4 最小出片验证
```bash
# 2.3 单路（B70 单卡：TDEV=CDEV=0）
HF_HUB_OFFLINE=1 LTX_TDEV=0 LTX_CDEV=0 LTX_GEMMA_DEVICE=xpu:0 LTX_GEMMA_OFFLOAD=cpu \
  .venv/bin/python -u run_t2v_xpu_perf.py      # → output/output_1024.mp4

# 2.5 单路
HF_HUB_OFFLINE=1 .venv/bin/python -u run_t2v_25_xpu.py
```

### 0.5 起服务
```bash
# 2.3：B70 单卡务必 LTX_MULTI_MODE=1
LTX_MULTI_MODE=1 LTX_HOST=127.0.0.1 bash start_ltx_server.sh    # :8001
# 2.5
bash start_ltx_server_25.sh                                     # :8002
```

### 0.6 恢复检查清单
- 单卡机器所有单路运行都要 `LTX_TDEV=LTX_CDEV=0`；`xpu:1` 不存在。
- 首次运行前建议跑一次 4096³ bf16 GEMM 探针，确认 ~150–175 TFLOPS。
- 无 `icpx`(2025.x) → SYCL 核编不过（自动回退 eager）；如需可 `make -C LTX-2/packages/ltx-kernels/csrc/sycl_r8 -j$(nproc)`。
- 重跑 `setup_env.sh` 会重置 `LTX-2/` 并重打补丁，属预期行为。

---

## 1. 环境与前置

- Python **3.12**，解释器统一用项目 venv：`<repo>/.venv/bin/python`（可用 `LTX_PYTHON` 覆盖）。
- 首次准备：

  ```bash
  cd /home/acm/paul_arc/ltx2.3-demo
  bash setup_env.sh          # clone LTX-2（固定 rev）、打补丁、uv sync、编译 R8 SYCL 核
  uv run download.py         # 下载 LTX-2.3 权重到 models/（~30-45 GB）
  ```

  - `setup_env.sh` 每次都会强制重置 `LTX-2/` 并重打补丁、重编 `sycl_r8`；`LTX_SKIP_KERNELS=1` 可跳过编译。
  - SYCL 核编译需要带 `libsycl.so.8` 的 **2025.x oneAPI 编译器**（`make` 会自动挑选；可用 `ONEAPI_ROOT`/`CC_DIR` 覆盖）。

- **LTX-2.5 权重**：解压/软链到 `<repo>/models/ltx-2.5`（或用环境变量 `LTX_25_MODELS` 指向 split pack 根目录）。
  找不到时 runner 会打印缺失文件清单并退出。

---

## 2. LTX-2.3 单路生成

```bash
cd /home/acm/paul_arc/ltx2.3-demo
HF_HUB_OFFLINE=1 LTX_TDEV=0 LTX_CDEV=0 \
  LTX_GEMMA_DEVICE=xpu:0 LTX_GEMMA_OFFLOAD=cpu \
  .venv/bin/python -u run_t2v_xpu_perf.py
```

- 输出默认：`<repo>/output/output_1024.mp4`。
- 常用变量：
  - `LTX_PROMPT`（提示词）、`LTX_WIDTH`/`LTX_HEIGHT`（必须能被 **64** 整除）、
    `LTX_FRAMES`（必须满足 **8k+1**，**切勿用 73**，会挂驱动）、`LTX_OUTPUT_PATH`。
- 也可用便捷脚本：`bash run.sh "你的提示词"`（已改为 `.venv`，可用 `LTX_PYTHON` 覆盖）。
- 多路（`run_multi_xpu.py` 8 路 / `run_multi_16.py` 16 路）**仅适用于多卡机器**；B70 单卡请用单路。

### LTX-2.3 实测（单路，1024×1024，121 帧，默认 prompt，seed 42）

现默认配置（eager + R2/R9A/R elementwise）：

| 阶段 | 时间 |
|---|---:|
| prompt-encode（Gemma-3，流式） | ~10–13 s（随主机负载波动） |
| stage-1 denoise（8 步 @512²） | 10.2 s |
| spatial-upsample 2× | 0.6 s |
| stage-2 denoise（3 步 @1024²） | 18.8 s |
| video+audio decode | 4.9 s |
| mux to mp4 | 5.1 s |
| **合计（TOTAL）** | **~52–53 s** |
| 峰值显存 | 18.16 GB |

对照：

| 配置 | 总时间 |
|---|---:|
| 最初未优化（首跑） | ~68.1 s |
| round-0（`LTX_ALL_ORIG=1`，关掉全部 R 优化） | ~61.2 s |
| 现默认（eager + R 优化） | ~52–53 s |
| `LTX_COMPILE=1`（编译；SYCL 会自动关） | ~57 s（不推荐，慢于 eager） |

---

## 3. LTX-2.5 单路生成

```bash
cd /home/acm/paul_arc/ltx2.3-demo
HF_HUB_OFFLINE=1 .venv/bin/python -u run_t2v_25_xpu.py
```

说明：
- 2.5 bf16 transformer 为 **42 GB**，载入时做 **fp8-cast（~21 GB 常驻）**；Gemma-4 文本编码器流式加载。
- 2.5 路径默认只启用**逐位安全**的优化：`LTX_R2_NOTILE`（去 tiling）、`LTX_R5_FUSE`、`LTX_R9A_FP8K`；
  R3/R6/R8/R9D/R10D 默认关闭（约 1 ULP，会被扩散放大而改变样本）。
- 常用变量：`LTX_PROMPT`、`LTX_WIDTH`/`LTX_HEIGHT`、`LTX_FRAMES`、`LTX_TDEV`、`LTX_OUTPUT_PATH`、`LTX_25_MODELS`。
- `LTX_PREBUILD_TRANSFORMER` 默认关（在 2.5 上并发构建可能触发 `DEVICE_LOST`）。

### LTX-2.5 实测（单路，1024×1024，121 帧，8+3 步，seed 42）

| 阶段 | 时间 |
|---|---:|
| Gemma-4 TE 构建 | 8.6 s |
| embeddings processor + prompt 编码 | 2.5 s |
| transformer fp8-cast 构建 | 6.4 s |
| stage-1 denoise（8 步 @512²） | 14.4 s（1.80 s/步） |
| stage-2 denoise（3 步 @1024²）+ decode | ~27 s（7.3 s/步 + ~5 s decode） |
| mux to mp4 | ~5.2 s（去 tiling 后；原 7.0 s） |
| **generation 计时** | **~56 s**（不含 mux） |
| wall（启动→落盘） | ~63 s |
| 峰值显存 | 25.28 GB |

### 2.5 vs 2.3 对比小结

同一台 B70、同为单路 1024×1024×121、8+3 步、seed 42：

| 维度 | LTX-2.3 | LTX-2.5 |
|---|---|---|
| 主权重 | fp8 单体（monolith） | bf16 42 GB → 载入时 fp8-cast ≈21 GB |
| 文本编码器 | Gemma-3-12B（~24 GB） | Gemma-4-12B（~26 GB） |
| 每任务固定开销 | TE 构建 ~10 s + 模型构建 ~4–6 s | TE 构建 8.6 s + 模型构建 6.4 s |
| stage-1（8 步 @512²） | 10.2 s | 14.4 s |
| stage-2（3 步 @1024²） | 18.8 s | ≈22 s |
| decode + mux | 4.9 + 5.1 ≈ 10 s | decode ≈5 s + mux 5.2 s |
| **合计** | **≈52–53 s** | **≈56 s（不含 mux）/ wall ≈63 s** |
| 峰值显存 | 18.16 GB | 25.28 GB |
| R elementwise 优化 | 启用（R3/R5/R6/R8/R9D/R10D） | 仅逐位安全项（R5/R9A）+ 去 tiling |

**要点**
- **2.5 比 2.3 慢约 4–6 s**：主要来自 stage-1/2 更慢（2.5 未启用 R elementwise 融合）以及更大的 Gemma-4、42 GB fp8-cast 构建。
- **2.5 显存占用明显更高**（25.3 GB vs 18.2 GB），但在 30.3 GiB 的单卡 B70 上仍可跑。
- **2.5 的价值在质量/功能**（音频、keyframes、时长头等），**不是速度**；要速度就用 2.3。
- 两者都保留 `LTX_ALL_ORIG=1` 全局回退；2.5 的单卡首任务开销（TE+模型载入 ≈15 s）目前无法摊薄（无常驻编码服务）。

---

## 4. Web 服务

两者共用同一套设计（FastAPI + SSE + SQLite 历史 + 网页 UI）。

### LTX-2.3 服务（默认端口 8001）

```bash
cd /home/acm/paul_arc/ltx2.3-demo
bash start_ltx_server.sh
```

- 默认：`LTX_HOST=0.0.0.0`、`LTX_PORT=8001`、`LTX_API_TOKEN=111`、`LTX_MULTI_MODE=8`。
- ⚠ **B70 单卡必须设 `LTX_MULTI_MODE=1`**（否则会按 device pair 起多个 worker，单卡上会失败）：

  ```bash
  LTX_MULTI_MODE=1 LTX_HOST=127.0.0.1 bash start_ltx_server.sh
  ```

- 打开 `http://<host>:8001/`；非 loopback 时需在页面填同样的 token。

### LTX-2.5 服务（默认端口 8002，单路）

```bash
cd /home/acm/paul_arc/ltx2.3-demo
bash start_ltx_server_25.sh
```

- 默认：`LTX_HOST=127.0.0.1`、`LTX_PORT=8002`、无 token（loopback）。LAN 访问：

  ```bash
  LTX_HOST=0.0.0.0 LTX_API_TOKEN=<token> bash start_ltx_server_25.sh
  ```

- 打开 `http://127.0.0.1:8002/`。
- 单卡一次只跑一个任务；2.5 每任务重载 TE（~8.6 s）+ transformer（~6.4 s）。任务被信号杀死时服务会**自动重试 1 次**。

### API（无 UI 提交）

```bash
# 提交（2.5：prompts 最多 1 条；2.3：最多 LTX_MULTI_MODE 条）
curl -s -X POST http://127.0.0.1:8002/api/multi-jobs \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"prompts": ["A red panda on a mossy branch, cinematic, 4k."]}'

# 查询
curl -s http://127.0.0.1:8002/api/multi-jobs/<job_id>

# 取视频（该端点无鉴权）
curl -s -o out.mp4 http://127.0.0.1:8002/api/multi-jobs/<job_id>/videos/0

# 进度事件（SSE）
curl -N http://127.0.0.1:8002/api/events
```

- 输出目录：2.3 `outputs/ltx-server/<job_id>/`；2.5 `outputs/ltx-server-25/<job_id>/`；
  每个任务内含 `video_i.mp4`、`video_i.log`、`results.json`、`prompts.json`。
- 注意：`/api/multi-jobs/{id}/videos/{i}` 端点**不做鉴权**（与原有设计一致）。

---

## 5. 常用环境变量速查

| 变量 | 默认 | 作用 |
|---|---|---|
| `LTX_TDEV` / `LTX_CDEV` | 0 / 1 | transformer / VAE 设备；**B70 单卡必须都设 0** |
| `LTX_GEMMA_DEVICE` / `LTX_GEMMA_OFFLOAD` | 单路 `cpu`；服务 `xpu:0` | 文本编码器设备；xpu 时建议 `cpu` offload（流式） |
| `LTX_WIDTH` / `LTX_HEIGHT` / `LTX_FRAMES` | 1024 / 1024 / 121 | 分辨率（÷64）、帧数（8k+1） |
| `LTX_OUTPUT_PATH` | `output/output_1024.mp4` | 单路输出文件 |
| `LTX_PROMPT` | 默认红熊猫提示词 | 提示词 |
| `LTX_25_MODELS` | `<repo>/models/ltx-2.5` | LTX-2.5 split pack 根目录 |
| `LTX_R2_NOTILE` | 1 | VAE decode 去 tiling（2.3/2.5 均生效） |
| `LTX_R9A_FP8K` | 1 | SYCL 向量化 fp8→bf16（逐位） |
| `LTX_R5_FUSE` | 2.3 开 / 2.5 开 | RoPE neg-fold（逐位） |
| `LTX_R3_FUSE` `LTX_R6_FUSE` `LTX_R8_SYCL` `LTX_R9D_K3V` `LTX_R10D_GATE` `LTX_R10D_GATE2` | 2.3 开 / 2.5 关 | elementwise 融合（2.3 有效；2.5 会改变样本故关） |
| `LTX_ALL_ORIG` | 0 | =1 时全局回退到未优化路径 |
| `LTX_COMPILE` / `LTX_COMPILE_MODE` / `LTX_INDUCTOR_CONFIG` / `LTX_DYNAMO_CONFIG` / `LTX_SEQ_DYNAMIC` / `LTX_FULLGRAPH` | 关 | torch.compile；**无法 trace SYCL 核**，开启会自动关闭 SYCL 相关 flag |
| `LTX_HOST` / `LTX_PORT` / `LTX_API_TOKEN` / `LTX_OUTPUT_DIR` / `LTX_DB` / `LTX_MULTI_MODE` | — | 服务配置 |
| `LTX_PYTHON` | `<repo>/.venv/bin/python` | 覆盖解释器 |

---

## 6. 故障排查

- **`UR_RESULT_ERROR_DEVICE_LOST` / `OUT_OF_RESOURCES` / `exec queue reset`**：
  多为 XPU 驱动瞬时状态。让 GPU 冷却或冷启动后再试；2.5 服务对信号崩溃的任务会自动重试 1 次。
- **查看 B70 温度**（本机 B70 = `card0` / `0000:05:00.0`）：

  ```bash
  for f in /sys/bus/pci/devices/0000:05:00.0/hwmon/hwmon*/temp2_input; do
    echo "$f = $(( $(cat "$f") / 1000 )) °C"   # pkg
  done
  ```

- **健康检查（GEMM）**：跑前可用一次 4096³ bf16 线性探针确认 ~150–175 TFLOPS。
- **重编 SYCL 核**：`make -C LTX-2/packages/ltx-kernels/csrc/sycl_r8 -j$(nproc)`。
- **单卡提示 `xpu:1` 不存在**：把 `LTX_CDEV` 设为 `0`。
- **`setup_env.sh` 重跑**会重置 `LTX-2/` 并重打补丁，属预期行为。

---

## 7. 端口与文件一览

- 8001：LTX-2.3 服务（`ltx_server.py`）
- 8002：LTX-2.5 服务（`ltx_server_25.py`）
- `<repo>/models/`：LTX-2.3 权重；`<repo>/models/ltx-2.5/`：LTX-2.5 split pack
- `<repo>/output/`：单路输出
- `<repo>/outputs/ltx-server/`、`<repo>/outputs/ltx-server-25/`：服务输出与日志
- `<repo>/patches/xpu.patch`、`<repo>/patches/r_kernels.patch`：环境与内核补丁
- `<repo>/B70使用说明.md`：本文件
